[phases.setup]
nixPkgs = ["python311", "libreoffice", "fontconfig", "freefont_ttf"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 300"
