[phases.setup]
nixPkgs = ["python311", "libreoffice-unwrapped", "fontconfig", "dejavu_fonts"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["fc-cache -fv"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 300"
