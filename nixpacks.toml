[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "python311Packages.gunicorn", "python311Packages.flask", "libreoffice", "fontconfig", "freefont_ttf"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 300"
